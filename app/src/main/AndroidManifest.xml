<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emergencyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".postDisasterReport.SubmitReport"></activity>
        <activity android:name=".postDisasterReport.MedicalStatus" />
        <activity android:name=".postDisasterReport.WalkThrough" />
        <activity android:name=".postDisasterReport.ReportActivity" />
        <activity android:name=".communities.CommunityProfile" />
        <activity
            android:name=".login.SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.ResetPasswordActivity" />
        <activity android:name=".communities.CommunitiesActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".HurricanePlan" />
        <activity android:name=".Plan_View_Page" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>